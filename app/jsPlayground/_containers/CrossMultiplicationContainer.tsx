import Reveal from '@/app/components/Reveal'
import CrossMultiplication from '../_components/CrossMultiplication'

const CrossMultiplicationContainer = () => {
  return (
    <Reveal delay={900}>
      <CrossMultiplication hideBrains />
    </Reveal>
  )
}

export default CrossMultiplicationContainer
